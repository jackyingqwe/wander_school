#include "heads.h"
int main()
{
    // 输出主菜单
    printMenu();
    // 选择功能（路线与景点两部分）
    // for (int i = 0; i < 100; i++)
    select_fun(show_filepath(4, 0));
    // 初始化道路
    // makeroad();
    // 初始化美景
    // init_dowandbeati();
    return 0;
}